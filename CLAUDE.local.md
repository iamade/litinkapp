# CLAUDE.local.md — `litinkapp/` (gitignored)

Personal / uncommitted overrides to this repo's `CLAUDE.md`. Gitignored — does not ship to GH.

## What goes here

- Active `.env` profile (`.env.local` vs `.env.docker`).
- Which Stripe key is wired locally (`STRIPE_SK_TEST` reference — by name, NOT value).
- Local Stripe webhook secret reference (set by `stripe listen` — never paste the `whsec_...` here).
- Personal sprint priorities, debug flags (`DEBUG_VIDEO_PIPELINE=1` etc.).
- Local feature toggles that differ from staging/prod.
- Notes on which Sprint 2 ticket (#1-#8) is in flight on this Mac right now.

## Status

(empty — populate as overrides accrue)
